import type { User } from "../types/user";
import strengthRoutine from "./routines";

const user: User = {
  name: "John Doe",
  age: 30,
  experienceLevel: "intermediate",
  routine: strengthRoutine,
};

export default user;
